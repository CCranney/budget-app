import React from 'react';
import { render } from '@testing-library/react';
import budgetReducer from './budgetSlice';

describe('initialize budgetSlice', () => {
    it('', ()=> {
        const budgetReducer = budgetReducer;
        /*
        const {  } = render(
            <Navbar />
          );
        */
    })
})